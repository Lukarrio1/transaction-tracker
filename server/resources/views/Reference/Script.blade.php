{{-- <script defer>

    const references_form = document.querySelector('#references_form')
    const triggers = document.querySelectorAll('.trigger')
    triggers.forEach(item => item.addEventListener('change', (e) => references_form.submit()))

</script> --}}
